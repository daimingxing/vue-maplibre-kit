# 样式工厂

适合谁读：需要快速配置点线面样式，或覆盖 MapLibre paint/layout 字段的开发者。

先读哪篇：[样式与状态索引](./index.md)。

对应示例：[NGGI02](../../../examples/views/NG/GI/NGGI02.vue)、[NGGI03](../../../examples/views/NG/GI/NGGI03.vue)。

## 简单样式

```ts
import {
  createSimpleCircleStyle,
  createSimpleFillStyle,
  createSimpleLineStyle,
} from "vue-maplibre-kit/business";

const pointStyle = createSimpleCircleStyle({
  color: "#f97316",
  radius: 7,
  opacity: 0.9,
  strokeColor: "#ffffff",
  strokeWidth: 2,
});

const lineStyle = createSimpleLineStyle({ color: "#0f766e", width: 4, opacity: 0.9 });
const fillStyle = createSimpleFillStyle({ color: "#79b8ff", opacity: 0.35 });
```

## 根据 properties 动态取值

简单样式工厂也可以接收表达式值。业务数据里已经有颜色、宽度、透明度字段时，优先用表达式工具读取 `properties`，不用直接手写原生表达式数组。

```ts
import {
  createSimpleCircleStyle,
  createSimpleFillStyle,
  createSimpleLineStyle,
  getFeatureColor,
  getFeatureNumber,
} from "vue-maplibre-kit/business";

const lineStyle = createSimpleLineStyle({
  color: getFeatureColor("color", "#1677ff"),
  width: getFeatureNumber("width", 3),
  opacity: getFeatureNumber("opacity", 0.8),
});

const pointStyle = createSimpleCircleStyle({
  color: getFeatureColor("color", "#409eff"),
  radius: getFeatureNumber("radius", 6),
});

const fillStyle = createSimpleFillStyle({
  color: getFeatureColor("color", "#79b8ff"),
  opacity: getFeatureNumber("opacity", 0.35),
});
```

对应 GeoJSON：

```json
{
  "type": "Feature",
  "properties": {
    "id": "fill_2",
    "color": "#79b8ff",
    "opacity": 0.35
  },
  "geometry": {
    "type": "Polygon",
    "coordinates": []
  }
}
```

`getFeatureColor("color", "#79b8ff")` 会优先读取 `properties.color`，没有该字段时使用默认色。

## 完整样式

```ts
import {
  createCircleLayerStyle,
  createFillLayerStyle,
  createLineLayerStyle,
  createRasterLayerStyle,
  createSymbolLayerStyle,
} from "vue-maplibre-kit/business";
```

这些工厂接收 `layout` 和 `paint` 的局部覆盖，并合并默认样式。

```ts
const lineStyle = createLineLayerStyle({
  layout: { "line-cap": "round", "line-join": "round" },
  paint: { "line-color": "#2563eb", "line-width": 5 },
});
```

## 选择建议

- 点线面快速业务图层：优先用 `createSimple*Style`。
- 需要完整 MapLibre 字段：使用 `create*LayerStyle`。
- 需要按 hover、selected 或业务字段变化：样式字段可以传表达式。
