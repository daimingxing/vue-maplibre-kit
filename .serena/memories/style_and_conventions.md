# 风格与约定
- 所有回复、文档和代码注释使用中文。
- 生成代码时要求函数级注释，必要时补充行间注释。
- 命名需简单易懂、语义清晰，不过长，不使用生僻词。
- 项目以 npm 组件库为优先目标，业务模拟页应通过公开门面消费能力。
- 门面导出集中在 `src/index.ts`、`src/geometry.ts`、`src/plugins/*.ts`。
- `src/views/NG/GI/NGGI00.vue` 被视为业务层示例，应尽量体现真实业务项目的低门槛接入方式。